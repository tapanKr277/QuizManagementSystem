import { DatetimeLocalPipe } from './datetime-local.pipe';

describe('DatetimeLocalPipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeLocalPipe();
    expect(pipe).toBeTruthy();
  });
});
